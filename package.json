{
  "name": "magicsell",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build && cd .. && Copy-Item -Path 'frontend/build/*' -Destination 'dist/' -Recurse -Force"
  },
  "workspaces": [
    "frontend",
    "api"
  ],
  "devDependencies": {
    "rimraf": "^6.0.1",
    "shx": "^0.4.0"
  }
}
