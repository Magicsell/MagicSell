{
  "name": "magicsell",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm ci && npm run build",
    "vercel-build": "cd frontend && npm ci && npm run build"
  },
  "dependencies": {
    "mongodb": "^6.3.0"
  },
  "devDependencies": {
    "rimraf": "^5.0.0"
  },
  "workspaces": [
    "frontend"
  ],
  "engines": { "node": ">=18" }
}
